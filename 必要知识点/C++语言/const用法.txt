const用法总结：
1、在指针和迭代器上；
	即设计为常量指针。
2、在指针、迭代器和reference指的对象上。
	即指针指向常量对象。
3、在函数的参数和返回类型上。
	一般有常引用，常引用指他所指的对象不允许修改。
	返回类型指不会返回左值。
4、在local变量身上。
	欲阻止一个变量被改变，可使用const，在定义该const变量时，需先初始化，以后就没有机会改变。
5、在成员函数身上。
	可以再成员函数后缀，代表函数内部不能修改成员变量。
	在声明和定义的时候都需要加上const关键字。