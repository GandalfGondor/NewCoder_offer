#include<iostream>

using namespace std;
int main()
{
	int cnt[2] = {0,0};
	int i = 0, j = 0;
	char *a = new char[5000];

	cin.getline(a,5000);
	while (a[j]!= NULL)
	{
		if (a[j] == ' ')
		{
			if (i == 0)
			{
				cnt[i] = j;
				i = 1;
			}
			else
			{
				cnt[i] = j;
				i = 0;
			}
		}
		j++; 
	}
	if (i == 0)
	{
		cnt[i] = j;
	}
	else
	{
		cnt[i] = j;
	}
    if (cnt[1] == 0)
	{
		cout << cnt[i]; return 0;
	}
	if (cnt[0] < cnt[1]) 
		cout << (cnt[1] - cnt[0]-1);
	else 
		cout << (cnt[0] - cnt[1]-1);
	return 0;
}
